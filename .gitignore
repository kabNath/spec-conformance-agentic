node_modules
.next

# Build artifacts
*.tsbuildinfo

# Python
__pycache__/
*.pyc

# Local recordings (source for docs/demo.gif — the GIF is committed, not the .mov)
*.mov

# Environment & secrets — never commit real keys
.env
.env.*
!.env.example

# Terraform — state and tfvars can hold plaintext secrets
*.tfstate
*.tfstate.*
infra/terraform/terraform.tfvars
.terraform/

# Keys / certificates
*.pem
*.key

# OS / editor cruft
.DS_Store

# 3GPP spec source (© 3GPP/ETSI) — local input, not redistributed
*.docx
examples/ts38331-source.txt
examples/ts38331-extract.txt

# Eval harness transient output (the pinned run1/run2 artifacts ARE committed)
eval/results.json
